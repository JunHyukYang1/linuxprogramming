실습과제 1번은 템플릿 클래스를 정의하여 여러 자료형으로 클래스를 사용할 수 있도록 만든 코드입니다. 먼저 템플릿 매개변수를 T1, T2 2개로 선언하고, 클래스는 private 멤버 변수로 data1과 data2를 가지게 합니다.
멤버 함수에는 생성자와 멤버 변수에 값을 저장하고 다시 반환하는 함수, 멤버 변수끼리 값을 교환하는 함수를 정의하였습니다. 이때 멤버 함수 중 값을 반환하는 함수는 반환해야 하는 값이 복수이므로 참조형 변수를 사용하였습니다.
값을 교환하는 함수는 같은 자료형의 변수를 선언해 data1의 값을 저장하고, data1에는 data2의 값을, data2에는 이전 data1의 값을 저장해둔 tmp의 값을 저장하도록 하여 값을 교환하였습니다. main 함수에서 템플릿 매개변수는
다양한 자료형으로 사용될 수 있으므로 위쪽에서는 int 자료형으로, 아래쪽에서는 double 자료형으로 사용되었습니다. 아래는 실행 결과입니다.


![image](https://github.com/user-attachments/assets/32d730eb-1fba-49b4-a489-70d812e42bb6)



실습과제 2번은 포인터 변수로 선언한 p에 동적 배열을 할당하여 알파벳 a ~ z의 데이터를 저장하고, 해당 데이터의 순서를 반대로 하여 출력하는 코드입니다. 먼저 클래스의 멤버 변수로 p를 포인터 변수로 선언, 배열의 크기를 저장하기 위한
size 변수를 선언하였습니다. 멤버 함수는 생성자와 소멸자, 배열에 값을 저장하는 함수, 배열에 저장된 값을 반환하기 위한 함수를 정의하였습니다. 생성자에서는 배열의 크기를 저장하는 size 변수를 초기화하고, 포인터 변수 p에 동적 배열을
할당하였습니다. 소멸자는 소멸 시 배열 p를 삭제하도록 정의하였습니다. main 함수에서 Container 클래스의 객체 c가 생성되었고, size는 26으로 초기화되었습니다. 이어서 for 반복문을 통해 배열 p에 알파벳 a ~ z가 순서대로 저장되었고,
다시 for 반복문을 통해 저장된 값의 순서를 반대로 하여 출력시켰습니다. 아래는 실행 결과입니다.


![image](https://github.com/user-attachments/assets/0e5256a6-c053-42c2-9616-426c48f9247d)
