실습과제 1번은 실수부(real)와 허수부(img)를 가지는 복소수를 클래스로 구현해 이에 연산자함수를 추가해 Complex 클래스 객체 끼리의 연산을 수행하기 위한 코드입니다.
먼저 Complex 클래스를 만들고 그 안에 멤버변수 real과 img를 만들고 멤버함수로 Complex 클래스의 기본 생성자를 만들고 객체 연산을 위한 연산자 재정의 함수, 결과를
출력하기 위한 show()함수를 선언합니다. 그 후 show()함수를 정의하고 연산자를 재정의햇습니다. 이때 Complex operation+ (Complex op2)와 같은 형태가 되는 이유는
피연산자 중 앞 쪽에 있는 객체는 '객체'.operation으로 사용되기 때문입니다. 즉, main 함수 안에 있는 sum = x + y는 sum = x.operation + y와 같은 취급입니다.
main 함수에서 x, y, sum 객체를 생성하고 sum = x + y를 수행한 후 sum에 저장된 값을 출력합니다. 아래는 실행 결과입니다.

![image](https://github.com/user-attachments/assets/ebd3355c-e402-4adb-a158-a1a7c7c918dd)


실습과제 2번은 실수부와 허수부를 가지는 복소수 끼리의 곱셈을 구현하기 위해 복소수 클래스의 멤버 함수로 *연산자를 재정의한 코드입니다. * 연산자를 재정의한 함수
내부를 보면 복소수끼리의 곱을 구현해 결과를 저장해 반환하는 형태입니다. 복소수끼리의 곱의 결과에서 실수부는 피연산자들의 실수부와 실수부의 곱, 허수부와 허수부의
곱을 더한 값이고 허수부는 피연산자들 간의 실수부와 허수부를 각각 곱한 값을 더한 것입니다. main 함수에서는 실습과제 1번과 같이 x, y, sum 객체를 생성 후
sum = x * y를 수행 후 결과를 저장한 sum 객체를 출력합니다. 아래는 실행 결과입니다.

![image](https://github.com/user-attachments/assets/612ed8cb-8431-4812-bcef-53a643f52299)


실습과제 3번은 kick과 punch라는 멤버 변수를 가지는 Power 클래스를 정의하고 멤버 함수로 Power 클래스의 기본 생성자의 정의, 객체와 정수 간의 연산을 위한 -연산자
재정의를 위한 함수 선언, 결과 출력을 위한 함수를 선언하였습니다. 이 후 결과 출력 함수 show()를 정의하고, -연산자 재정의를 위한 함수를 정의하였습니다.
이 정의에서 Power opeartion- (int op2)는 객체와 정수간의 연산을 의미합니다. 이때 객체는 반드시 피연산자 중 앞에 위치해야 하고 정수는 반드시 뒤에 위치해야 합니다.
재정의한 함수는 객체가 가진 각 변수에 정수만큼의 값을 -연산한 것을 결과로서 출력하도록 정의되어 있습니다. main 함수는 객체 a와 b를 생성하고 생성 직후의 값을 한번
출력한 후 b = a - 2를 수행한 후 다시 한번 객체 a와 b를 출력합니다. 아래는 실행 결과입니다.

![image](https://github.com/user-attachments/assets/47fc7d21-e84b-48c9-b740-5fd0aba6124b)
