실습과제 1번은 vector 템플릿 클래스의 객체를 생성해 사용자로부터 정수 4개를 입력받고 알고리즘 템플릿 함수를 이용해 객체의 원소 중 최대값과 최소값을 구하여 distance()함수를 이용해 최대값과 최소값이 위치한 주소를 함께 출력하였습니다.
그 후 객체의 원소들을 오름차순 정렬 후 순서대로 출력한 후 이를 다시 내림차순 정렬 후 순서대로 출력하였습니다. 아래는 실행 결과입니다.

![image](https://github.com/user-attachments/assets/12ed5f98-188e-441e-9ce4-9bac413b7d85)


실습과제 2번은 vector 클래스의 객체 sv를 생성해 사용자로부터 5개의 string 자료형의 문자열을 입력 받고 이를 객체의 원소로 저장 후 알고리즘 함수인 sort를 이용해 오름차순 정렬하여 사전 상 단어의 순서대로 정렬하였습니다. 그 후 가장
마지막에 위치한 원소, 즉 사전 상 가장 뒷쪽에 위치한 단어를 출력하였습니다. 아래는 실행 결과입니다.

![image](https://github.com/user-attachments/assets/3474510f-50ff-4df3-ae1c-2553f0500717)


실습과제 3번은 vector 클래스의 객체 sv를 생성해 사용자로부터 5개의 string 자료형의 문자열을 입력 받고 이를 객체의 원소로 저장 후 알고리즘 함수인 sort를 이용해 오름차순 정렬하여 이를 출력하였습니다. 아래는 실행 결과입니다.

![image](https://github.com/user-attachments/assets/256a58c3-8a59-417c-8837-bfd02b702cfd)


실습과제 4번은 점수를 비교하여 가장 높은 점수를 받은 학생의 이름과 점수를 출력하는 코드입니다. 먼저 이름과 점수를 멤버 변수로 가지는 Student 클래스를 정의하고, 멤버 함수로 클래스의 생성자와 후에 객체 간의 비교 연산을 위한 < 연산자
재정의를 하고, 결과 출력에 사용할 멤버 함수도 정의하였습니다. main 함수에서는 먼저 vector 클래스의 객체 st를 생성하고, vector 클래스에 종속된 iterator 클래스의 객체 it를 생성하였습니다. 그 후 사용자로부터 이름과 성적을 한쌍으로
하여 3명 분량의 값을 입력 받고 이를 각각 Student 클래스의 객체로 생성하여 객체 st의 원소로 저장하였습니다. 그 다음에는 알고리즘 함수인 max_element()를 사용해 최대값을 가지는 원소의 주소를 반환 받아 it에 저장하였는데 이때 객체 간의
연산을 위해 위에서 < 연산자를 재정의하여 점수 간의 비교 연산으로 최대값을 가지는 객체를 지정하였습니다. 마지막으로 멤버 함수로 정의하였던 show()함수를 이용해 결과를 출력하였습니다. 아래는 실행 결과입니다.

![image](https://github.com/user-attachments/assets/d2aa7048-210b-4d72-ada3-28cf017d9ca7)
