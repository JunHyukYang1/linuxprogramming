실습과제 1번은 템플릿 함수를 이용해 각 자료형의 배열 내에서 최대값을 구해 출력하는 코드입니다. 템플릿 함수는 자료형만 다르고 함수 내부의 코드 내용이 같은 함수들을
하나로 통합시켜 코드의 길이를 줄일 수 있게 한 것입니다. 템플릿 함수에서 T는 템플릿 매개변수로 이는 int, char, double 등의 자료형들로 선언한 매개변수와 같다고 취급합니다.
템플릿 함수로 각 자료형 내의 배열 중 최대값을 구해 반환하도록 정의하여 결과를 얻었습니다. 또한, 출력을 담당하는 코드 부분의 함수에 <자료형>을 넣어 템플릿 매개변수가 어떤
자료형으로 사용되었는지 명시적으로 표기하였습니다. 아래는 실행 결과입니다.


![image](https://github.com/user-attachments/assets/5aa07a69-57aa-4cf3-9249-226bf6c82f57)



실습과제 2번은 멤버 변수 radius를 가지는 Circle 클래스를 정의하고, 멤버 함수로 > 연산자를 재정의해 템플릿 함수에 객체가 대입되었을 때 원하는 결과를 얻을 수 있도록 작성한
코드입니다. 먼저 Circle 클래스의 내부에 생성자와 radius를 반환하는 함수, 연산자 재정의 함수를 선언 및 정의하고, 템플릿 함수를 정의해 2개의 변수 또는 객체를 비교하여 보다
큰 값을 가지는 쪽을 반환하여 출력하도록 하였습니다. 이때 객체의 경우 객체가 가지는 멤버 변수 radius에 저장된 값을 비교해 보다 큰 객체를 반환하도록 하였습니다.
아래는 실행 결과입니다.


![image](https://github.com/user-attachments/assets/64bd253d-a5b5-4494-85c7-f7ca4c2c3523)
