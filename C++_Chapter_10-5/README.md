실습과제 1번은 값을 설정한 2차원 배열 2개 간의 + 연산을 수행해 각 원소끼리의 합을 원소로 가지는 배열을 출력하는 코드입니다. 먼저 생성과 함께
2차원 배열을 가지는 클래스를 정의하였습니다. 생성자에 배열의 값을 초기화하는 코드를 넣고, + 연산자를 재정의하여 객체가 가진 2차원 배열에서 같은 행,
같은 열에 위치한 원소끼리의 + 연산을 수행한 후 이를 2차원 배열의 형태로 출력하도록 각각 함수를 정의하였습니다. main 함수에서는 초기화할 값을 넣어서
객체를 2개 생성하고 객체 연산 후 결과를 출력하였습니다. 아래는 실행 결과입니다.


![image](https://github.com/user-attachments/assets/cf1b04c7-9de6-4191-847e-76d0c413e338)


실습과제 2번은 3명의 학생들의 국어, 영어, 수학의 성적을 2차원 배열에 저장하고 이들 중 가장 높은 점수를 받은 학생과 점수의 평균을 구해 출력하는 코드입니다.
먼저 2차원 배열을 선언한 후 초기화하고, 3명의 성적을 입력하여 2차원 배열에 저장합니다. 그리고 vector 클래스의 객체인 v를 생성해 객체에 각 학생들의 성적 평균을
원소로 하여 저장합니다. 그 후 알고리즘 함수를 이용해 성적 평균 중 최대값을 가지는 학생이 누구인지와 최대값을 출력하였습니다. 아래는 실행 결과입니다.


![image](https://github.com/user-attachments/assets/0359e321-76f9-4edd-9ae0-da1288cb165d)


실습과제 3번은 2차원 배열을 선언함고 동시에 배열의 값을 초기화 해주고 배열에 저장되어 있는 값 중 최대값을 찾아 값이 몇인지와 어느 행, 어느 열에 있는지를
출력하는 코드입니다. 먼저 2차원 배열의 선언 및 초기화를 진행하고, for 반복문을 통해 최대값을 구하고, 최대값이 위치한 행과 열을 각각 변수에 저장하였습니다.
마지막으로 최대값과 행과 열을 저장한 변수를 사용해 결과를 출력하였습니다. 아래는 실행 결과입니다.


![image](https://github.com/user-attachments/assets/b848b045-ebde-4e6f-b721-6f36e33ed472)


실습과제 4번은 2차원 배열 선언 및 초기화를 한 후 배열 내의 원소 중 음수인 값은 0으로, 양수인 값은 255로 초기화 시킨 후의 배열을 출력하는 코드입니다.
선언 및 초기화 한 배열의 원소는 (-5, 2, 35, -20, 5, 100, -75, 5, -25)입니다. for 반복문과 if문을 활용해 음수인 값은 0으로, 양수인 값은 255로 초기화하였습니다.
그 후 3x3 배열의 형태로 출력하였습니다. 아래는 실행 결과입니다.


![image](https://github.com/user-attachments/assets/e39d101d-bd03-4bb0-8489-b0f525534db4)
