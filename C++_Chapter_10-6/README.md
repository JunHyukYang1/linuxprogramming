실습과제 1번은 2개의 2차원 배열끼리의 연산을 수행해 결과를 출력하는 코드입니다. 먼저 2개의 2차원 배열을 생성과 함께 초기화합니다. 그 후 for 반복문을 이용해 같은 행, 같은 열에 위치한 배열의 원소끼리 + 연산을 수행해 그 값을 미리
선언해둔 변수에 저장하고 이를 원소로 하여 선언해둔 1차원 배열에 저장합니다. 그 다음 이 1차원 배열 자체를 원소로 하여 2차원 배열에 저장하고 결과를 출력하였습니다. 아래는 실행 결과입니다.

![image](https://github.com/user-attachments/assets/78b56384-3289-4d1a-8db3-247308c49d52)


실습과제 2번은 3명의 학생이 받은 세 과목의 성적의 평균을 계산해 가장 높은 점수를 받은 학생과 그 성적을 출력하는 코드입니다. 가장 먼저 학생의 성적을 구별하여 저장하기 위한 2차원 배열을 생성하고, 나중에 평균 성적을 저장하기 위한
1차원 배열도 생성합니다. vector 클래스에 종속된 iterator 클래스의 객체 it은 후에 최대값을 구하기 위해 생성하였습니다. 이제 사용자로부처 학생의 성적을 3개씩 3명분 받은 후 이를 2차원 배열에 저장합니다. 그 후 행을 구분, 즉 학생별
성적 평균을 계산해 미리 선언해둔 1차원 배열 객체에 저장합니다. 생성했던 it 객체와 알고리즘 함수를 이용해 1차원 배열 내에서 최대값이 위치한 주소를 구하고, 해당하는 학생과 그 성적 평균을 결과로 출력하였습니다.
아래는 실행 결과입니다.

![image](https://github.com/user-attachments/assets/d90d25f4-a126-4789-a95a-bad24f774979)


실습과제 3번은 생성과 초기화를 함께 수행한 2차원 배열 내에서 최대값을 찾아내고, 해당 값이 위치한 행과 열을 결과로 출력하는 코드입니다. 먼저 v.at(0).at(0)을 최대값을 비교하기 위한 초기값으로 설정하고, for 반복문을 통해 배열 내의
원소들과 값을 비교해가며 최대값을 구하였습니다. 이때 최대값을 구함과 동시에 해당 값이 위치한 행과 열 또한 미리 선언해둔 변수에 저장하여 결과 출력에 사용하였습니다. 아래는 실행 결과입니다.

![image](https://github.com/user-attachments/assets/f0ba64ee-00d9-4d3c-a336-5cd880b152ae)


실급과제 4번은 생성과 초기화를 함께 수행한 2차원 배열 내의 원소 중 음수인 값은 0으로, 양수인 값은 255으로 바꾼 후 결과를 출력하는 코드입니다. 먼저 2차원 배열을 생섬 및 초기화하고 for 반복문을 이용해 음수인 값은 0으로 바꾸고,
양수인 값은 255로 변환하였습니다. 변환 후의 배열 또한 for 반복문을 이용해 2차원 배열의 형태로 출력하였습니다. 아래는 실행 결과입니다.

![image](https://github.com/user-attachments/assets/eeb53091-40d1-4e59-9298-f3f21ff1e5ad)
